# VittoriaDB RAG Backend Configuration with Datapizza AI

# === EMBEDDINGS CONFIGURATION (Datapizza AI) ===
# Documentation: https://docs.datapizza.ai/0.0.2/Guides/RAG/rag/
# Choose embedding provider: "openai" or "ollama"
EMBEDDER_PROVIDER=openai

# OpenAI Embeddings (when EMBEDDER_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_EMBED_DIMENSIONS=1536
# Optional: Custom OpenAI endpoint (leave empty for official API)
OPENAI_BASE_URL=

# Ollama Embeddings (when EMBEDDER_PROVIDER=ollama)
# Requires Ollama running locally with embedding models installed
# Install model: ollama pull nomic-embed-text
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_EMBED_DIMENSIONS=768

# === VITTORIADB CONFIGURATION ===
VITTORIADB_URL=http://localhost:8080

# === GITHUB INTEGRATION ===
# GitHub Token (optional, for private repository indexing)
GITHUB_TOKEN=your_github_token_here

# === SERVER CONFIGURATION ===
PORT=8501
HOST=0.0.0.0

# === LOGGING ===
LOG_LEVEL=INFO

# === NOTES ===
# Background tasks are handled by FastAPI BackgroundTasks (no Redis needed)
# For Ollama embeddings: Make sure Ollama is running and models are downloaded
